// ⚠️ DEPRECATED: Ce fichier est conservé pour la rétrocompatibilité
// Utilisez les imports depuis './api' ou les services spécifiques à la place
//
// Migration guide:
// - import { login, getSites } from './api/client' 
// + import { login, getSites } from './api'
//
// Ou pour plus de clarté:
// + import { login } from './api/auth.service'
// + import { getSites } from './api/sites.service'

// Réexportation de tous les services pour la rétrocompatibilité
export * from './index';
